Заметки к занятиям:

Урок 1. Основы, знакомство с языком

   1. Вводные замечания
      - представиться, варианты коммуникаций (группа в телеграме)
      - посещение занятий, выполнение дз, ежедневные упражнения с кодом
      - дополнительное чтение, файл рекомендаций
      - рекомендованные книги в браузере
   2. Основная тема
      - скачиваем и устанавливаем Go
        - переменная среда GOPATH
        - командная строка Go: run, build, get, install, test
      - напомнить про git
      - скачиваем и устанавливаем Visual Studio Code
        - обзор настроек и панелей
        - плагин Go
        - hello.go, разбираем программу и запускаем её
        - подготовка папки курса
      - пакеты и импорт
        - пакет fmt, функции Print(), Printf() и Scan()
        - пакет math
      - переменные, константы и указатели
        - добавим переменную в hello.go
        - именование переменных
        - области видимости
        - зарезервированные слова
        - два способа объявления переменных
        - указатели и функция new()
      - фундаментальные типы данных: числа, логические значения, строки
        - операции с типами данных

Урок 2. Функции, условия, циклы. Пишем простые программы

   1. Разбор домашнего задания
      - комментарий в начале кода
      - максимальная простота исполнения
      - использование Printf()
      - использование простых функций
   2. Основная тема
      - циклы (for)
        - использование счетчика или условия
        - обход по сущности (range), строка, UTF-8, пустой идентификатор
      - ветвления (if, switch)
      - функции
        - передача параметров (по значению, по ссылке), переменное число
        - возврат значения, нескольких значений
        - функция внутри функции, замыкания
        - рекурсия
        - отложенный вызов (defer)
        - ошибки выполнения, функции panic() и recover()

Урок 3. Массивы, срезы, карты, структуры, JSON

   1. Разбор домашнего задания
      http://demin.ws/blog/russian/2010/03/06/what-is-faster-on-eratosthenes-seive-go-c-cpp/

   2. Основная тема

Урок 4. Методы и интерфейсы

   1. Разбор домашнего задания
   2. Основная тема

Урок 5. Стандартная библиотека: time, string, os, io/ioutil, flag, log, encoding/XML
Урок 6. Стандартная библиотека: testing, net/http, html, image, reflect
Урок 7. Горутины и каналы
Урок 8. Горутины и каналы, практика